# 🎬 Moteur de Recommandation de Films (TMDB)

Projet d'analyse sémantique de synopsis utilisant le NLP (BERT), l'optimisation vectorielle (FAISS) et une architecture API (FastAPI).

## 🛠️ Installation et Configuration

Pour garantir le bon fonctionnement du projet en équipe, suivez scrupuleusement les étapes ci-dessous.

Installation des dépendances
Installez toutes les bibliothèques nécessaires avec une seule commande :

```bash
pip install --upgrade pip
pip install -r requirements.txt
```
